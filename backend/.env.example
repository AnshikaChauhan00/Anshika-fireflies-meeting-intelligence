# SQLAlchemy database URL. SQLite file lives inside backend/ by default.
DATABASE_URL=sqlite:///./meetings.db

# Optional: enables the LLM-powered "Ask about this meeting" chat (Groq).
# Leave empty and the chat still works using keyword search over the transcript.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
